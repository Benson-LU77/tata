# Tata 上架待辦

> 更新於 2026-08-30。程式碼與素材已備妥;打勾的是完成,方框的是待辦。
> 標「**你**」的是只有你能做的(涉及 Apple ID 與付款,我不碰)。

---

## A. 送出前(依序)

- [ ] **你** — Xcode → Settings → Accounts 登入 Apple ID
- [ ] **你** — 開 `ios/App/App.xcodeproj` → Signing & Capabilities → 選 Team
      (Bundle ID 已設好:`page.tata.app`,自動簽名)
- [ ] **你** — App Store Connect 建立 App 記錄,Bundle ID 選 `page.tata.app`
- [ ] **你** — 真機測試:接上 iPhone → Xcode 直接跑一次
      (這一步也是下面 C 段真機驗證的入口)
- [ ] Xcode → Product → Archive → Distribute → App Store Connect

## B. App Store Connect 欄位(內容都在 `docs/appstore-copy.md`)

- [x] 名稱、副標、中英描述
- [x] 關鍵字、Support URL、隱私政策 URL(https://tata.page/privacy/ 已上線)
- [x] 類別 Lifestyle / Productivity、年齡分級 4+、免費無內購
- [x] 審查備註(對 4.2 的正面說明:完整互動像素遊戲 + 原生檔案 + 全離線)
- [x] App Privacy 問卷答案:全部 **Data Not Collected**
- [x] 截圖 6.9 吋五張(`/Users/a77/Claude/Tata-appstore/6.9-inch/`,1320×2868 無 alpha)
- [ ] 6.5 吋截圖(若 App Store Connect 要求;6.9 吋通常可涵蓋)

## C. 真機驗證(P2 資料夾橋 — **裝上實機才能做**)

> v1 已收掉這個功能,所以以下**不擋送審**。它們是 v1.1 的開門條件。

模擬器沒有 iCloud Drive、也不能點系統選擇器,以下每一項都必須真機。

- [ ] 選資料夾:picker 打得開、選得到 iCloud 雲碟
- [ ] 三條結束路徑都不卡:選取 / 取消 / **下滑關閉**
- [ ] 重開機後書籤仍可用(失敗時退避重試 0.5/2/5 秒後成功)
- [ ] 未下載檔的真實形式:是 `.X.md.icloud` 隱藏 stub,還是以真名出現
- [ ] 未下載檔 read → 回「還在下載中」而**不是**空白頁
- [ ] 飛航模式下對未下載檔存檔 → 內容**未被覆寫**、草稿保留
- [ ] **entitlement(關鍵)**:TestFlight build 呼叫 `startDownloadingUbiquitousItem`
      不報錯。若報錯,退路是改用 coordinated read 隱式觸發下載
- [ ] 與 Obsidian 併寫:Tata 開著某頁 → Obsidian 改同一檔 → 存檔應跳衝突卡
- [ ] Obsidian 不會因為我們寫檔而關掉分頁(就地非 atomic 寫入的目的)
- [ ] 非 ASCII 檔名往返(vault 裡放一個 macOS 建的 `café.md`)
- [ ] 換機還原:顯示「請重新選資料夾」,不崩、不靜默退回

## D. 範圍決定(**你**拍板)

- [x] ~~v1 是否先收掉「選一個資料夾」~~ → **已收掉**(2026-08-30 決定)
      `FOLDER_PICKING_SHIPPED = false`(app/components/notes.tsx)。P2 的程式碼
      一行沒刪,只是不出場;殼上實測齒輪隱藏、卡片為空,網頁三條路不受影響。
      C 段真機驗證走完之後,把那個開關翻成 true 就是 v1.1。
- [ ] LICENSE:repo 目前沒有授權檔。開源(MIT?)或保留版權由你決定。

## E. 已完成(存檔備查)

- [x] Capacitor 殼 + Swift Vault plugin(list/read/write,App Documents)
- [x] Release 組態驗證可編譯、可執行
- [x] iPhone only、直向鎖定(橫向版面在 720/900 斷點間會破碎)
- [x] icon 與啟動畫面(像素天際線,1024 不透明 / 2732)
- [x] 隱私政策頁上線
- [x] 首航稽核:歡迎卡不再宣稱有 Obsidian、空保管庫不再顯示「連接」
- [x] 三座橋的「空清單/不存在」謊言全部修正(Swift list、Swift read、REST list)
- [x] P2 四步程式碼完成(IO queue、picker+書籤、檔案協調+placeholder、暖機+安全搬家)

---

## 之後(v1.1 起)

- iPad 原生版面(實測可跑,但沒為它設計過)
- iCloud 衝突版本偵測(`NSFileVersion.unresolvedConflictVersionsOfItem`,只提示不自動解)
- NSFilePresenter:Obsidian 改動時即時刷新(取代輪詢)
- 對比度掃描(約 20 處偏灰的字)
- 打字效能量測(目前沒有證據顯示有問題,但也沒量過)
